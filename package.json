{
  "name": "cscom",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run index.ts",
    "serve": "bun run src/transport/server.ts",
    "typecheck": "tsgo --noEmit",
    "build": "tsgo --noEmit && bun build --compile index.ts --outfile dist/cscom && bun build --compile src/transport/server.ts --outfile dist/cscom-serve && cp src/transport/dashboard.html dist/",
    "start": "./dist/cscom",
    "start:web": "./dist/cscom-serve"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript/native-preview": "^7.0.0-dev.20260628.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
